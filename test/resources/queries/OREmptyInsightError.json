{
  "errorMsg": "OR must be a non-empty array",
  "title": "should return InsightError for empty OR",
  "input": {
    "WHERE": {
      "OR": []
    },
    "OPTIONS": {
      "COLUMNS": [
        "courses_dept",
        "courses_avg"
      ],
      "ORDER": "courses_avg"
    }
  },
  "errorExpected": true,
  "with": "InsightError"
}